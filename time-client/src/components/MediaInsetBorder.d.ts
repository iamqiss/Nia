import type React from 'react';
import { type ViewStyleProp } from '#/alf';
/**
 * Applies and thin border within a bounding box. Used to contrast media from
 * bg of the container.
 */
export declare function MediaInsetBorder({ children, style, opaque, }: {
    children?: React.ReactNode;
    /**
     * Used where this border needs to match adjacent borders, such as in
     * external link previews
     */
    opaque?: boolean;
} & ViewStyleProp): any;
//# sourceMappingURL=MediaInsetBorder.d.ts.map